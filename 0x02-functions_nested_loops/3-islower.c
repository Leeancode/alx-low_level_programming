#include "main.h"
/**
*_islower - function to check if a character is lower or upper case.
*@c: The character to be checked.
* Return: 0 success.
*
*/
int _islower(int c)
	if (c >= 'a' && c <= 'z')
{ return (1);
	else
		return (0);
}
